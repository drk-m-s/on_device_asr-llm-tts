# Core audio IO
sounddevice==0.4.6
numpy==1.26.4
scipy==1.11.4

# VAD (fast, offline)
webrtcvad==2.0.10

# Offline ASR (choose one)
# Option A: faster-whisper (recommended for accuracy; you must have the model files locally)
faster-whisper==1.0.3
# If you prefer tiny CPU-only, set the env var FAST_WHISPER_MODEL_DIR to a local path.
# OR Option B: Vosk (very light, purely CPU; uncomment these two lines and comment out faster-whisper)
# vosk==0.3.45
# requests==2.32.3

# Local LLM HTTP client
requests==2.32.3

# Offline TTS
# Option A: pyttsx3 (fully offline, built-in voices)
pyttsx3==2.90
# Option B: Piper CLI (better quality) -> you install piper binary yourself; we’ll call it via subprocess.
soundfile==0.12.1

# (Optional) small utils
pydub==0.25.1
